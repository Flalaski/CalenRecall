# Assets Directory

Place your application icon here:
- `icon.png` - Application icon (256x256 or larger recommended)
- `icon.ico` - Windows icon file (optional, for better Windows integration)

For now, the application will use a default icon if these files are not present.

